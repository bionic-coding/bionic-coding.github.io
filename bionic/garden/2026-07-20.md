---
date: 2026-07-20
high_water:
  commit: 6c16601876b6ec59cabd170c9dc562706d92c848
  dirty: ""
  log_head: "## [2026-07-20] garden | morning note 2026-07-20 (3 headlines, 1 artifact)"
  journal_head: "## [2026-07-17 17:17] implementation | Processed the 2026-07-17 gardener inbox — order fix, next-issue queue, link-checker merge"
items:
  - id: garden-improvement-pb0001-ledger-reconcile
    kind: improvement
    headline: "PB-0001 is done on the site but open in the ledger — the nav fix shipped in commits while the run snapshot still says prompt 3, in-progress"
  - id: garden-research-mcp-lesson-freshness
    kind: research
    headline: "Escalating once: the MCP spec is final-dated Jul 28 (8 days out) and your mcp.md predates it — a light pass keeps the flagship honest and anchors the next issue"
  - id: garden-idea-model-plus-harness-lesson
    kind: idea
    headline: "The 'model + harness' dyad is now the industry's standard shape — which is your thesis in the wild, and a strong lesson angle"
---

## Since your last move

You built. Nine commits on the 18th, no ceremony: a README rewrite, a new
scaffolded `tools.md` (promo space commented in, entries hand-written — exactly
the scaffold-don't-generate move), a deliberate de-AI-ing pass on titles and
emdashes, and — the headline of the batch — **the broken header nav is fixed.**

- **Mobile nav shipped.** A real hamburger button (SVG, `aria-expanded`,
  `aria-controls`), a small vanilla-JS toggle that closes on link-tap / Escape /
  outside-click, and `@media` styling in `main.scss`. I verified it: the build is
  green, and both the restored **About** page (fixed via the `about.markdown` →
  `about.md` rename that was the actual bug) and the new **Tools** page render and
  sit in `header_pages`.
- **The one real bug I flagged last pass is closed** — the `order: 4` collision;
  `leveraging-ai` → 10, `glossary` → 11. Clean 1–11 now.

So the *work* is done and it's good. The one thing that didn't come with it is a
paper trail — which is the whole of my first headline.

## Headlines

**1. PB-0001 is finished on the site but still open in its own ledger.**
`garden-improvement-pb0001-ledger-reconcile` · You spun up the `iterate` cycle
[[promptbooks/active/PB-0001-fix-mobile-nav-and-restore-about]] to fix exactly this
nav problem — ran the verify + council diagnosis (prompts 1–2 `done`) — and then
fixed it *in commits*. The fix is live and correct. But RUN-001 is frozen at
**prompt 3, `running`, `in_progress`**, with the dev + review + summary prompts
still `pending`. So the run ledger now claims work is mid-flight that actually
shipped two days ago. Two clean ways to close the gap: advance the run to reflect
reality (`run-promptbook` — mark the dev prompt `done` with the commits as
artifacts, run the review, archive), or if the commits deliberately bypassed the
cycle, mark it abandoned. Either is a minute of work; leaving it open is the kind
of drift that makes the next cycle's status untrustworthy. Not urgent — but it's
the one thing on the whole tree that says one thing and means another.

**2. The MCP spec is final-dated July 28 — 8 days out. (Escalating, once.)**
`garden-research-mcp-lesson-freshness` · Last pass I carried this with a clock and
said I'd escalate it once if it went untouched. It did, so here's the once. The
`2026-07-28` spec is now confirmed final (RC locked May 21): a **stateless core**
(remote servers behind a plain load balancer, cacheable `tools/list`), an
**Extensions** framework, **Tasks**, **MCP Apps** (server-rendered HTML UIs in
sandboxed iframes), auth hardening, and a formal deprecation policy. Your `mcp.md`
is dated `2026-07-17` and doesn't know any of this. It won't *break* — it's
conceptual — but a flagship lesson that's a beat behind the biggest revision since
launch is worth a light freshness pass before the 28th. This is *also* the anchor
story for your next weekly issue; the two are distinct (one keeps the evergreen
lesson honest, the other is the dated column). Both are in tonight's drop. After
this I rest it — your judgment stands.

**3. "Model + harness" is now the industry's standard shape — and that's your
thesis, out loud.** `garden-idea-model-plus-harness-lesson` · Fresh this week: as
of Jul 19, every major lab ships **both** a model *and* an agent harness — the
model supplies judgment, the harness supplies tools, permissions, parallel
workers, persistence, a place to run. That is *precisely* what bionic-coding
argues: augmentation over autonomy, the human holding the permissions, the
scaffolding mattering as much as the model. It's a strong new Learn angle — "why
the harness matters as much as the model," a natural neighbor to your agents and
MCP lessons — or a column frame. I've parked it in the drop as grist. If it reads
lesson-worthy to you, it's the kind of thing worth a whiteboard before a word gets
written.

## Also noticed

- **Kimi K3's receipts fully landed** — `garden-news-next-issue-candidates` · #4 on
  the Artificial Analysis Intelligence Index (57.11, behind Fable 5 and GPT-5.6 Sol,
  above Opus 4.8), and **#1 on the Frontend Code Arena** at 1679 Elo. Weights drop
  Jul 27. This closes the honest asterisk your Jul 17 issue left open; it's queued
  in tonight's drop for the week-of-Jul-27 issue. Also fresh grist for the resting
  "why leaderboards lie" angle (#4 on one board, #1 on another) — still living as
  *evidence inside an issue*, not a standalone, per your signal.
- **The 07-18 buildout has no journal entry.** `garden-improvement-buildout-journal`
  · Nine commits including a genuine editorial decision (the de-AI-ing pass — pulling
  emdashes and AI-tell titles is a *stance*, not a typo fix). Cleanup didn't flag it
  (those aren't accept/archive ops), but a two-line `log-work` entry would capture
  *why* you de-AI'd — the kind of reasoning that's invisible in a diff and gold in six
  months.
- **News sources — here are the three I'd accept first.** `garden-improvement-review-seed-sources`
  · Still all `(proposed)`, so tonight was snippet-only WebSearch again. I've noted
  this twice softly; escalating to concrete instead of repeating: accept just
  **Simon Willison** (highest-signal hands-on), **Model Context Protocol** (you have
  an MCP lesson *and* the Jul 28 spec), and **Anthropic News**. Remove `(proposed)`
  from those three rows in `docs/garden/sources.md` and I can read them in full for
  cleaner column receipts. Resting after this.
- **`whats_next` is quiet.** Cleanup ran tonight — same five P2s, all "ADR-000N not
  linked from README/USER_GUIDE," all optional for a content site. No P1s. PB-0001 is
  *not* flagged stuck (it moved on the 18th, inside the active-progress window) — the
  ledger mismatch in headline 1 is a real-vs-recorded gap, not a staleness one.

## Artifacts

- [[inbox/gardener-next-issue-candidates-2026-07-20]] — refreshed next-issue queue:
  MCP final-spec receipts (anchor), firmed Kimi K3 numbers, and the new "model +
  harness" frame flagged as possibly bigger than one issue.

## Back-notes

- **Escalated to resolution, once:** `garden-research-mcp-lesson-freshness` — carried
  with a Jul-28 clock last pass; escalated tonight as promised. Resting after this;
  your call stands.
- **Escalated from soft to concrete:** `garden-improvement-review-seed-sources` — I
  stopped re-noting "sources are all proposed" and named the three specific rows worth
  accepting. Resting after this pass regardless of the answer.
- **New:** `garden-improvement-pb0001-ledger-reconcile` (the run/reality mismatch) and
  `garden-idea-model-plus-harness-lesson` (the thesis-in-the-wild lesson angle).
- **Still resting:** `garden-idea-benchmark-split-lesson` — more K3 grist arrived
  (#4 vs #1), but per your signal it stays *evidence inside an issue*, not a standalone.
- **Standing, not re-offered:** the `garden/link-check-lessons` branch is still yours
  to merge when you want it — status, not a nudge.
- **Retired last pass, staying retired:** the ship-a-timely-article thread and the
  link-checker "want me to draft it?" offer. Not resurfacing.
