# Journal — 2026-08

_Append-only. Newest entries at the top._

## [2026-08-14 18:12] review | PB-0001/RUN-002: mobile-nav verify cycle converged after 3 review rounds

Fix was the mobile nav card + About link on the Jekyll site, run verify-only (RUN-002) because the user had already committed both root fixes before the run started: ff17186 (header_pages `about.markdown` → `about.md`) and a77ecf2 (tap-target + surface-panel CSS/JS). A 3-round council backed the verify-only posture — round 2 unanimously rejected a diagnosis that still framed committed work as future work, round 3 unanimously approved the re-baselined version.
Verification found the About link already worked: ff17186 had already fixed the stale `about.markdown` entry, so the issue's premise was partly stale by the time we checked it. The docs audit went further and empirically falsified the kramdown blank-line gotcha as written in root CLAUDE.md — tested against the repo's real toolchain, the actual failure is a leak triggered by indentation or a missing blank line, not "stops converting for the rest of the file."
Biggest surprise: a 23-path Chrome-emulation fixture harness (V10) scored 0 DEFECT, but round-3 testing against the real built `_site/` found the shipped code mis-navigating on 3 of the site's own pages — the fixture's "harmless" verdict on that exact tap was itself a fixture artifact (a non-interactive spacer sat where a real link sits on the live pages). The fix that actually killed the defect class was moving the panel out of flow at ≤46rem, not patching more guards onto the reflow.
The exposure audit also caught README/USER_GUIDE/CLAUDE.md publishing into `_site` (MX-4) — fixed via `exclude:`, with a published-file-set regression unit (V7) recorded because the fix landed in the very file the cycle was certifying.
False comments surfaced three separate times across rounds (the `_config.yml` header_pages comment, root CLAUDE.md's "reached via the site title" claim, the pointerdown comment's self-contradicting clause) — a recurring defect class, not one-off drift.
Next time: test interaction bugs against the real built site, never a fixture alone; verify doc/comment claims empirically before trusting them as contracts; treat any exclude-list change as needing its own published-file-set regression unit; a true iOS WebKit device check is still recommended before this one merges.
Refs: [[promptbooks/PB-0001-fix-mobile-nav-and-restore-about]] [[adrs/ADR-0005-serve-a-landing-homepage-and-a-five-item-top-nav]]

## [2026-08-14 08:36] decision | PB-0001/RUN-002: commit to verify-only approach for the mobile-nav fix

Root causes (historical, pre-fix): four defects — stale exact-path string in `_config.yml` header_pages (`about.markdown` vs `about.md`) defeating the exact-path Liquid lookup; `.nav-toggle` tap target ~31.6px, below the 44px floor; collapsed mobile menu never styled as a surface; toggle missing `type="button"`, Escape-close, outside-click-close. All four were already fixed and committed before this run: S1 in ff17186; S2/S3/toggle in a77ecf2 ("scss fixes", landed during RUN-001 before its abandonment).
Chosen approach: verify-only — confirm the committed implementation meets every diagnosed requirement via `jekyll clean` + build + rendered-output checks (incl. the production-overlay build per ADR-0003), then document. Re-implement nothing; do not replace the committed inset-panel variant (the book-goal FALLBACK).
Why right: the round-2 council's unanimous MUST-FIX proved that treating committed work as future work would have made the cycle re-apply or overwrite it; round 3 approved the re-baselined diagnosis unanimously (confidence 0.973, CONVERGED).
Non-obvious things the council surfaced: (1) the abandoned RUN-001 had already completed its implementation phase — only verification and documentation remained; (2) without `jekyll clean`, the stale `.jekyll-cache` re-renders the old 4-link nav and false-negatives the About-link check.
Refs: [[promptbooks/PB-0001-fix-mobile-nav-and-restore-about]] [[adrs/ADR-0003-ship-a-custom-presentation-layer-over-minima]]
