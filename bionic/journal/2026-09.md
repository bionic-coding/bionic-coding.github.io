# Journal — 2026-09

_Append-only. Newest entries at the top._

## [2026-09-15 14:45] implementation | Built /crux/team/ and /crux/catalog/ from the crux learning-catalog package

Set out to turn the crux 3.16.1 learning-catalog handoff (10 roles, 60 skills) into two public pages in the site's crux section — a teaching page and a generated reference — without hand-maintaining an inventory anywhere.
First payload export carried `source.revision` pointing at a private authoring revision instead of a released one; caught on review and fixed by regenerating from the released plugin distribution (`~/.claude/plugins/cache/crux/crux/3.16.1`) so `source.revision` is null. That's the kind of drift a generated-content page can ship silently if nobody checks where the export ran against.
Verified the regenerated payload byte-identical to the delivered one across `agents`, `skills`, `counts`, `teaching_model`, `audience_definitions`, `journeys`, `handoff_template`, and `publication_notes`, with matching `manifest_sha256` / `guide_sha256` / `exporter_sha256`, and confirmed `--check` exits 0 against that distribution — worth doing explicitly rather than trusting the export script's own success message.
Ran two independent reviews before calling it done: one on constraints/data, one on prose against the seven writing rules. Both found things to fix, which is the point of running them separately rather than skimming once and moving on.
Next time: check which plugin distribution an export script resolves against *before* running it, not after a reviewer flags the revision field.

Friction: initial catalog export resolved against a private authoring revision instead of the released plugin distribution; required a re-export to fix `source.revision`.

Refs: [[adrs/ADR-0003-ship-a-custom-presentation-layer-over-minima]]
