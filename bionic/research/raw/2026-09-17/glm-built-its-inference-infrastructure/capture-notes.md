# Capture provenance

The direct URL fetch on 2026-09-17 returned an empty JavaScript shell. That attempt remains unchanged in `source.html` and `source.url`.

The user subsequently pasted rendered HTML into the conversation. `submitted.md` transcribes one complete article instance from that supplied content, preserving article wording, headings, lists, code, captions, and figure URLs. Repeated DOM copies, navigation, scripts, styles, and footer were omitted. It is a Markdown transcription, not an original HTML export. The named Downloads file still contained only the shell when inspected; it was left untouched.

The four article figures are fetched separately from the URLs in the supplied HTML. The audited source page uses local figure links. Publication date comes from the supplied page header; Z.ai is the corporate author, with no individual byline present. The source is filed as a fixed publication.
