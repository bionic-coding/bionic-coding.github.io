---
date: 2026-07-14
high_water:
  commit: 4574762a17ddbdde9bc0d23e6e7d968b50adab9e
  dirty: a9f11a7a60a99ae4e3a187ab626e0f4e49396f7f53945b87d82b093f358e1710
  log_head: "## [2026-07-14] garden | morning note 2026-07-14 (3 headlines, 1 artifact)"
  journal_head: "## [2026-07-13 13:30] learning | Soft-launch retrospective — ADR-0001–0004 in hindsight"
items:
  - id: garden-news-model-drafts-unblocked
    kind: news
    headline: "Both facts your model-news drafts left as TODO are now public with primary sources — inbox drop ready"
  - id: garden-idea-benchmark-split-lesson
    kind: idea
    headline: "The benchmarks don't crown a winner — that split IS your 'benchmarks mislead' lesson, handed to you"
  - id: garden-improvement-review-seed-sources
    kind: improvement
    headline: "Accept a few news sources so next pass I can capture for you, not just point via snippets"
  - id: garden-improvement-lesson-references-consistency
    kind: improvement
    headline: "The Markdown lesson still has no References section (resting)"
  - id: garden-improvement-lesson-ia-drift
    kind: improvement
    headline: "Lesson tree (11) still drifted from the brief's 8 (resting)"
  - id: garden-engineering-gap-link-checker-ci
    kind: engineering-gap
    headline: "No link/citation checker in CI yet (resting until a draft with live citations ships)"
---

## Since your last move

You moved, and it was the big one. Two nights ago I led with three things; you came
back and made the top two *real*.

- **The "cite through crux" promise is kept.** You ran the sourcing pipeline for the
  first time — **five sources** ingested (Opus 4.8, Fable 5, Simon Willison's review,
  the GPT-5.6 system card rendered out of an 81-page PDF, and GPT-5.6 pricing), with the
  `frontier-models-2026` reference now carrying verified dates, tiers, and prices. That
  was my #2 headline last night ("the pipeline has never run for a lesson or article").
  Retiring it — it's done.
- **All three model-news drafts are seeded** with cited facts and honest _unverified_
  flags, prose left to you. Exactly the scaffold-don't-generate split you want.
- **The process debt cleared itself in one sweep:** the launch retrospective is journaled
  (ADR-0001–0004, and a genuinely reflective one — the contrast-churn and the taxonomy
  rename, not a summary), `audit-docs` ran for its first baseline, and you turned off the
  unused `code` concern. `whats_next` is down to five optional P2s.
- And this morning you're sharpening the **My AI Kit** post — the "If you only copy one
  thing" → "The TLDR" rename. Small, but it's your *durable-intent / disposable-code*
  thesis applied to content: the post is a living thing you keep tuning, not a monument.

Marker valid (last note's garden op corroborates it) — this is a normal turn, no
corruption.

## Headlines

**1. The two things your drafts leave as TODO are now public — and I put the sources in your inbox.**
`garden-news-model-drafts-unblocked` · Last night I flagged two _Open / unverified_ items
in the synthesis. Both surfaced this week with citable primary sources, so I dropped a
fact sheet — [[inbox/gardener-model-news-followup-2026-07-14]] — for you to dispatch when
you're ready (I didn't ingest it; that's your morning gate).
- **The Fable "why" is on the record.** Anthropic's own *Redeploying Claude Fable 5* post
  reconciles the contradiction I couldn't: the June 12 pull **was** a US export-control
  directive — triggered by an Amazon jailbreak report where Fable produced vulnerability-
  exploit code — **and** the "enhanced safeguards" framing is the fix that got it back
  (a new classifier, restored July 1). Both halves are true. The human-interest hook your
  *Fable, Relaunched* draft wanted is real and sourceable now.
- **The head-to-head benchmarks exist.** Terminal-Bench, SWE-Bench Pro, and the Artificial
  Analysis index are all public — enough to finally fill the comparison table with dated,
  cited numbers.

**2. The benchmarks don't name a winner — stop looking for one, and teach the split.**
`garden-idea-benchmark-split-lesson` · Here's what the numbers actually say: **Sol wins
Terminal-Bench** (88.8 vs Opus 78.9), **Fable wins SWE-Bench Pro** (80.3 vs Opus 69.2 vs
Sol 64.6), and the **Artificial Analysis Intelligence Index is a near-tie** (Fable ~60,
Sol 59) — at a third of Fable's cost. There is no "best model," and that is not a gap in
your reporting, it's the *whole lesson*. Your own My AI Kit post already calls the tier
tangle "confusing"; this is the companion piece — *why the leaderboard you saw on Twitter
is lying to you*. It's grounded, it's on-audience, and it writes itself from the fact sheet.

**3. Accept a few sources so next time I can read, not just point.**
`garden-improvement-review-seed-sources` · I'm escalating this from last night's tail
because tonight showed the cost: every source in `sources.md` is still `(proposed)`, so my
news pass was snippet-only — I could find the Fable post and the benchmarks but not *read*
and capture them for you. Remove `(proposed)` from Anthropic, OpenAI, and Artificial
Analysis (that edit is the acceptance gate and seeds the egress allow-list), and next pass
I can hand you captured, cited sources instead of a lead sheet.

## Also noticed

- **Still open from last night, low-effort, resting (not repeating):** the Markdown lesson
  still has no `## References` section (`garden-improvement-lesson-references-consistency`),
  and the lesson tree is still 11 files against the brief's 8
  (`garden-improvement-lesson-ia-drift`). Both are convenience-when-you-touch-them, not
  blockers — I'll stay quiet on them until they matter.
- **Link-checker, resting.** `garden-engineering-gap-link-checker-ci` · Your drafts now
  carry `[src: …]` tags and the published lessons will carry live outbound URLs soon. When
  the first draft with real citations ships, an html-proofer step is worth adding — I'll
  offer to draft it on a `garden/` branch then. Not yet.
- **`whats_next` is clean.** Five P2s remain, all "link ADR-000N from README/USER_GUIDE" —
  genuinely optional for a content site. Dismiss them in `tending.md` if cross-linking
  isn't a goal and I'll stop counting them.

## Artifacts

- [[inbox/gardener-model-news-followup-2026-07-14]] — the two-keeper fact sheet (Fable
  suspension cause + head-to-head benchmarks, with primary sources to capture). Dispatch
  via `process-inbox` / `ingest-research` when you're ready.

## Back-notes

- **Watch resolved (from 2026-07-12):** I said I'd escalate the *Fable, Relaunched* hook if
  it were still a TODO. It is — but not because you stalled; you seeded the draft, and now
  the primary source for the hook exists. So I escalated it into a concrete inbox drop
  rather than repeating the nudge.
- **Escalated:** `garden-improvement-review-seed-sources` moved tail → headline, with the
  cost now demonstrated.
- **Closed by you (retired):** `garden-improvement-crux-sourcing-pipeline` and
  `garden-improvement-journal-and-audit-debt` — both done; celebrated above, won't resurface.
- **Rested:** the three plain-language post ideas from last night folded into the
  benchmark-split lesson (headline 2); the durable/disposable content policy — you're
  living it (the My AI Kit edit), so I'll stop pitching it as an idea.
- **Held:** the weekly-freshness whiteboard. Resting it deliberately — the lessons are days
  old, nothing's stale yet. I'll offer to actually run the session (unattended, lands in
  your inbox) once a lesson's `updated:` date starts to age past the news.
