# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you:
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: Bionic Coding
description: >- # this means to ignore newlines until "baseurl:"
  Durable intent, disposable code.
baseurl: "" # site is served at the domain root
url: "https://bionic-coding.com"
github_username: bionic-coding

# Build settings
theme: minima
plugins:
  - jekyll-feed

# Markdown: GitHub-flavored (tables, strikethrough, task lists), with
# hard_wrap off so hand-wrapped paragraphs don't become <br> tags.
markdown: kramdown
kramdown:
  input: GFM
  hard_wrap: false

# Collections. "lessons" is the AI Literacy section — ordered reference pages
# served at /learn/<name>/. "lesson_drafts" mirrors _drafts for posts:
# work-in-progress lessons that render in local dev but are excluded from the
# production build (see _config_production.yml, used by the deploy workflow).
collections:
  lessons:
    output: true
    permalink: /learn/:name/
  lesson_drafts:
    output: true
    permalink: /learn/:name/

# Default layout for lessons (renders the "Last reviewed" date).
defaults:
  - scope:
      path: ""
      type: lessons
    values:
      layout: lesson
  - scope:
      path: ""
      type: lesson_drafts
    values:
      layout: lesson

# Top-nav pages. The manifesto is the homepage (linked via the site title).
header_pages:
  - manifesto.md
  - articles.md
  - learn.md
  - tools.md
  - about.markdown

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
exclude:
  - docs/
  - _templates/
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
